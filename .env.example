# Optional but recommended in production:
# Used for absolute canonical/OG/Twitter URLs in metadata.
# Example: https://codedash.example.com
NEXT_PUBLIC_SITE_URL=

# AI configuration is managed in My Page (/settings).
# No AI-related environment variables are required.
